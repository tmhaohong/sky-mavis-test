const baseUrl = 'http://murmuring-wildwood-66916.herokuapp.com';

const URLS = {
  WALLET_NAME: `${baseUrl}/api/walletName`,
  UNLOCK: `${baseUrl}/api/unlock`,
  SEND_ASSET: `${baseUrl}/api/assets/send`,
};

export default URLS;
